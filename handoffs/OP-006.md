# OP-006 交接：工单、身份与稳定工具网关（已完成）

## 当前实际结果

- 基线为 `c1dd538`；所有者已完成 OP-006 最终 review，`TASKS.md` 中 OP-006 为 `DONE`。本地已创建 OP-006 提交，当前方向为不 push。`tasks/OP-006.md` 逐条记录九项验收的实现位置、测试位置与实测结果。
- API 不再信任 `X-OpsPilot-Actor-Id`。`HmacBearerAuthenticator` 对 HS256 签名、固定 issuer/audience、`exp`、`nbf`、`sub`、`jti` 做确定性验证；验证后的 subject 才从数据库加载组织、角色、target 和 resource 授权。subject 全局唯一，避免多组织同名主体产生歧义。没有签发 token 的业务端点。
- 新增迁移 `0002` 和 `User`、`UserTargetScope`、`TargetResource`、`UserResourceGrant`、`SupportCase`、`CaseMessage`。工单创建由组织内唯一 dedupe 键与 savepoint 处理竞争；命中既有 case 时重新校验其 target/resource，并拒绝换 target/resource 复用同一 key。状态转换使用带 version/status 条件的单条 SQL UPDATE；当前只有 OPEN、DIAGNOSING、WAITING_USER、ESCALATED，没有 RESOLVED/CLOSED、动作或批准路径。创建/成功转换追加 AuditEvent，CaseMessage 由数据库 trigger 保持 append-only。
- 固定 12 个已支持稳定只读工具：connector/application/runtime health、instance summary，以及 database/dataset/chart/dashboard 的 list/get。ToolDefinition/Binding/CatalogSnapshot seed 可重复并检测契约/映射漂移；MCP 目录是完整不可信观察，只有稳定契约中明确列出且实际观测到的 allowlist 名称才能生成 binding。
- Gateway 在 Provider 调用前验证固定输入 Schema、认证 target/resource 和数据库契约哈希；调用后验证 32 KiB 上限、输出 Schema、health scope、resource type、返回 resource 映射与用户 grant。list 只返回授权项，get 必须精确返回所请求的内部 resource。请求/响应审计摘要限制深度、条目数和字符串长度，并按敏感键脱敏。
- 每一个 Gateway binding 尝试均新增一个 append-only ToolExecution 和一个关联 AuditEvent，包括成功、拒绝、Provider 缺失、MCP 默认禁用、目录漂移、认证/连接/超时/Schema/超限失败和后续回退；最终成功保存此前失败链，Provider correlation 进入审计事件。
- `SupersetRestProvider` 只允许固定 GET 路径；`RuntimeProbeProvider` 只接受 worker/beat/redis 的预配置 TCP endpoint；`SupersetMcpProvider` 只接受固定 allowlist、Bearer、超时、32 KiB 响应与绑定目录哈希。MCP 默认关闭。

## 验证证据

- 常规后端：`uv --cache-dir .cache/uv lock --check`、`python -m ruff check src tests`、`ruff format --check src tests`、`mypy src tests`、`pytest -q` 全部通过；最终为 **38 passed / 8 skipped / 91.75% coverage**，唯一 warning 是既有 Starlette AnyIO alias deprecation。Gateway/seeding 依赖真实 OP-004 数据库 trigger，因此从无数据库的普通 coverage 分母排除，并由下述显式 PostgreSQL 回归覆盖，不能把该排除误称为单元覆盖。
- 显式 PostgreSQL：固定隔离实例 `opspilot-op006-postgres-1`、回环端口 55433；`alembic downgrade base`、`upgrade head`、`alembic check` 完整重放通过，结果为 `No new upgrade operations detected`；`pytest tests/integration --no-cov -q` 为 **8 passed**。测试包含真实两线程/独立 Session 的 case dedupe 与 version 竞争、签名 API 主体、两组织/多用户、跨 target/resource、CaseMessage/OP-004 audit append-only、12 个稳定工具逐项调用和 23 次失败/拒绝/回退审计链。
- 真实 Superset 6.1.0 `target-mcp` profile：用当前进程短期 access token 运行 `labs/op006/verify_target.py`，application health 为 `HEALTHY`，instance 为 Apache Superset 6.1.0，database/dataset/chart/dashboard list 均为 HTTP 成功且当前 0 项；输出没有 token 或资源内容。该 profile 没有可信的 worker/beat/redis 独立 liveness endpoint，三个 Probe 均如实返回 `UNKNOWN`。
- 同一真实 profile 显式提供短期 MCP Bearer 并运行 `--mcp-url http://127.0.0.1:5008/mcp --expect-mcp-error RESPONSE_TOO_LARGE`：受控客户端在目录超过 32 KiB 时返回 `REJECTED/RESPONSE_TOO_LARGE`。默认结果仍为 `mcp_policy_default=DISABLED`；这进一步执行本地拒绝边界，不改变 OP-003 的原生只读目录和上游大小 Gate **FAIL**。
- 前端（未改业务代码）：`pnpm --dir frontend install --frozen-lockfile`、`format:check`、`lint`、`typecheck`、`build` 全部通过；Next.js 16.3.3 production build 生成 `/` 与 `/_not-found`。
- 收尾检查：`git diff --check` 通过；高置信 credential 扫描无命中；tracked/unignored 文件无超过 1 MiB；`skills/`、`knowledge/`、`evidence/` 及 OP-003/004/005 交接对 HEAD 均无 diff。

## 明确保留的边界

- OP-005 的 `scheduled-report-triage` 冻结 Skill 引用了 report schedule/history 稳定名，但真实 `target-mcp` profile 未启用 reports，`/api/v1/report/` 实测为 404，且当前没有经验证的历史响应契约。因此 OP-006 没有把这两个语义注册为“已支持”；该 Skill 在相关稳定观察实现前不能形成完整闭环。冻结 Skill 文件及其哈希未修改。
- 真实 Superset 当前四类元数据列表为空，所以真实 profile 只证明固定 REST endpoint、认证、响应解析和范围语义；四类 get 的非空转换、内部 resource 映射与逐用户返回授权由受控 Provider 加实际 PostgreSQL Gateway 测试证明，没有伪造目标资源。
- 未实现 Agent/LangGraph、模型调用、Skill 执行、报告调度观察、审批、目标写动作、验证、Specialist、业务 UI、SSE 或最终应用镜像。这些不能从 OP-006 推导为完成。
- 本机仍运行已批准的 `opspilot-op006` PostgreSQL 和 OP-003 `target-mcp` 容器供 review；未删除卷，未触碰用户其他 Docker 资源。

## 完成状态

所有验收项、代码、测试和实测证据已完成并经所有者 review；OP-006 为 `DONE`，本地提交已创建。按当前方向不得 push，不得开始 OP-007，且不得改变 OP-003 MCP FAIL、12 个稳定只读边界或 OP-005 冻结内容。
