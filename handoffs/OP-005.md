# OP-005 交接：知识、混合 RAG、Skills 与独立评测

## 结果

- 新增 `knowledge/sources.json`、8 份最小语料、12 条冻结查询真值和 `knowledge/README.md`。官方材料只有链接与短小派生笔记；5 个官方条目记录 URL、2026-09-05 访问日、6.1.0 范围、许可/再分发边界和内容 SHA-256。3 份 Runbook 明确为合成项目演示资料。
- 新增 `src/opspilot/knowledge/`：Markdown 标题/代码块/表格/配置/连续步骤的父子切块、文档版本及检索版本入库、BGE-M3 1024 Dense 与 token-aware Sparse、严格范围过滤检索、错误/配置/任务精确通道、pgvector 精确 Dense、Sparse、RRF、按需 Reranker、父块上下文和可定位 Citation。
- 没有新迁移：OP-004 的 `KnowledgeDocumentVersion`、`RetrievalIndexVersion`、`KnowledgeChunk` 契约已足够。入库先创建 `BUILDING` 索引，父块无表示、子块以同索引的 Dense/Sparse 版本 `READY`，再转换为 `READY`；现有不可变性与表示触发器继续生效。
- Windows 本机加载复用 OP-001 已验证原则（fast `tokenizer.json`，legacy `.bin` 显式 `mmap=False`），但正式代码不导入 spike。项目锁定 `torch==2.8.0+cpu` 指向 PyTorch CPU 官方索引；此前由通用 PyPI 解析的 2.14 在本机 `c10.dll` 加载失败，已替换。
- 三个固定 Skill 位于 `skills/`：数据库连接、访问控制、定时报表。`FixedSkillRegistry` 只接收这三个仓库目录，验证 JSON-compatible YAML 前置元数据、版本/语义内容哈希、启用状态、适用版本、唯一匹配和稳定工具 allowlist；不会下载、执行、注册或调用工具。

## 实际 RAG 消融

历史报告：`evidence/OP-005_RAG_ABLATION_2026-09-05.json`（逐例、无失败）及 `.metadata.json` 保留为首次运行记录，不是提交前 review 修复后的权威结果。正式项目 `.venv`，12 条冻结查询，同组织/目标/索引/来源类型/可见性/6.1.0 过滤；CPU 串行。

提交前 review 修复后的权威重跑为 `evidence/OP-005_RAG_ABLATION_2026-09-06.json` 和 `.metadata.json`：该 metadata 记录基线 HEAD `27b06315e9d694830b69b83f458fa2b8a5396e10`、检索索引 `c506c5f1-6b96-4dd7-83d9-7e3475d30503`、未提交工作树及 SHA-256 指纹 `2663d6370a1ba0e0dc064210e9d4f7f937709e46bd5037433aadf7483c234c3b`。它不改写 `knowledge/` 的冻结语料或 12 条查询真值。

| 组别 | Recall@3 | MRR | nDCG@3 | P50 / P95 | 峰值 RSS |
| --- | ---: | ---: | ---: | ---: | ---: |
| Dense | 1.000 | 0.958 | 0.949 | 206 / 221 ms | 1.31 GB |
| Dense + 精确 | 0.917 | 0.778 | 0.765 | 207 / 223 ms | 1.30 GB |
| Dense + 精确 + Sparse/RRF | 1.000 | 0.833 | 0.850 | 207 / 255 ms | 1.30 GB |
| + Reranker | 1.000 | 0.958 | 0.969 | 3,095 / 8,944 ms | 5.10 GB |

所有组的父块上下文命中和引用有效率均为 1.000。当前小语料没有证明精确或 Sparse+RRF 的净排序收益；Reranker 有 0.020 nDCG 增益但显著增加 CPU 延迟和内存，故实现保留、默认关闭并按需加载。不得将这 12 条合成/派生检索样本解释为真实企业准确率。

2026-09-06 重跑指标保持相同的 Recall/MRR/nDCG 结论：Dense P50/P95 为 261/278 ms、Dense+精确 259/293 ms、Hybrid 279/306 ms、Reranker 4,063/11,549 ms；Reranker 峰值 RSS 5.10 GB。各组 12/12 逐例结果无失败；Reranker 逐例状态均为 `APPLIED`。加载/评分失败的 `FALLBACK` 路径由真实 PostgreSQL 回归以故障 Reranker 覆盖，而非在冻结评测中伪造失败。

## 提交前 review 修复

- 每个 `Citation` 和 `RetrievalResult` 显式带 `retrieval_index_version_id`，实际 PostgreSQL 测试断言结果和所有引用都等于请求的冻结索引。
- Reranker 关闭返回 `DISABLED`；启用成功为 `APPLIED`；模型加载或打分异常为 `FALLBACK`，保留完全相同的 Hybrid RRF 候选排序/分数/通道并给出降级原因，不虚构 reranker 通道。
- 超过配置上限的不可分结构单元不硬切：其 child 保留完整内容和原始行定位，并记录 `oversized_structure_unit`、配置上限和实际长度。这是有意的结构/定位优先支持边界。
- Skill 回归新增未知稳定工具、错误目标版本、错误触发，以及三个 Skill 各自停止/升级与禁止动作断言；没有新增 Provider、注册或调用。

## 验证

- 正式 `.venv`：`torch 2.8.0+cpu`、`transformers 4.57.6`、`psutil 7.2.2`；实际 BGE-M3 输出 1024 Dense 和非空 Sparse。
- 最终权威 PostgreSQL 验证（2026-09-06）：隔离 Docker 项目 `opspilot-op005` 通过 `alembic upgrade → downgrade base → upgrade`、`alembic check` 和实际 pgvector 回归（1 passed）。该回归断言入库、冻结索引 Citation、Reranker off/on/failure，以及 organization、target_system、retrieval_index_version、visibility、source_type、target_version 任一范围不匹配均返回空结果。
- 最终权威常规后端验证（2026-09-06）：`uv --cache-dir .cache/uv lock --check`、Ruff、format、`mypy src tests` 和 `pytest -q` 通过；`pytest` 为 26 passed / 3 skipped、94% coverage（数据库集成仍显式运行，不能由普通 pytest 代替）。此前 23 passed / 3 skipped、93% 是 review 修复前历史记录，不用于提交判断。
- 最终权威前端验证（2026-09-06）：`pnpm --dir frontend install --frozen-lockfile`、format check、lint、typecheck 和 production build 均通过。唯一输出警告为既有 Starlette deprecation warning。
- 本次只改实际 PostgreSQL 回归和交接文字，未重跑 BGE 消融；2026-09-06 冻结语料/查询的权威评测报告未受行为实现改动影响。`opspilot-op005-postgres-1` 仍健康运行，未执行 Docker 清理或卷删除，供所有者复核；评测数据和 JSON 证据保留在明确命名的 OP-005 资源中。
- `models.py`、`ingestion.py`、`retrieval.py` 从无 PostgreSQL/无本机模型的普通 coverage 分母排除，因其在实际 PostgreSQL 与 BGE 消融入口独立验证；不要把该排除误称为未覆盖路径的成功。

## OP-006 硬边界

- 可复用 `RetrievalService`、冻结索引和 `FixedSkillRegistry`；RAG 返回事实/Citation，Skill 返回方法契约，二者均不能授予权限或形成当前目标系统结论。
- 未创建 `ToolDefinition` 种子，未连接/调用 MCP、REST 或 Probe，未实现认证、RBAC、工单、Agent、HITL、Specialist 或任何业务 API/UI。OP-006 才能实现稳定工具注册、Gateway、Provider 选择、授权、审计和降级。
- OP-003 原生 MCP 的只读目录和响应大小闸门仍是 FAIL；不得因本任务提供的稳定工具名称而声称 MCP 已安全接入。
- 本任务未提交、未 push。项目所有者确认 diff 后才允许本地提交。
