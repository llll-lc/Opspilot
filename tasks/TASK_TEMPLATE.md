# OP-XXX：任务名称

状态快照（非权威，以 `TASKS.md` 为准）：`NOT_STARTED`

## 目标

一句话说明本任务解决的业务或工程问题。

## 必须先读取

- 仓库入口、当前任务依赖、相关规格/ADR 和前置交接。

## 前置检查

- Git、现有实现、测试、环境和依赖任务。
- 防重复搜索与已有失败记录。

## 范围

列出允许完成的工作。

## 不在范围

列出容易顺手做、但本任务明确禁止的工作。

## 实现与安全约束

- 直接引用相关规格或 ADR，不复制整段内容。
- 写明资源、权限、幂等、来源、兼容性和数据真值等特殊约束。

## 验收条件

每一项必须能通过测试、场景断言、命令输出、报告、截图或文件检查验证。

## 需要用户参与

只列出账号、密钥、下载/付费批准、外部发布和主观验收等必须由用户完成的事项。
