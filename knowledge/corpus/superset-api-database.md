# Database API metadata

## Scope before lookup

Database metadata is an authorized resource lookup. The local retrieval note only describes locating a scoped database record and never authorizes export, creation, update, SQL execution, or connection-string disclosure.

## Failure interpretation

A filtered list or not-found response can be an authorization outcome. Preserve the response scope and timestamp as evidence instead of converting it into a general claim about target-system health.
